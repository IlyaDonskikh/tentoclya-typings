import { CoreUserProvider } from "../../models/core/userProvider";

export interface BrokerCoreUserProviderCreatedPayload {
  userProvider: CoreUserProvider;
}

export interface BrokerCoreUserProviderUpdatedPayload {
  userProvider: CoreUserProvider;
}
